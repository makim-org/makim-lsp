{
  "name": "language-server",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/server.js",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json && mkdir -p dist/schema && cp -f src/schema/makim.schema.json dist/schema/makim.schema.json",
    "test": "vitest run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.10.0",
  "dependencies": {
    "@types/node": "^24.0.3",
    "ajv": "^8.17.1",
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.12",
    "yaml": "^2.8.0"
  },
  "devDependencies": {
    "@types/ajv": "^1.0.4",
    "vitest": "^3.2.4"
  }
}
